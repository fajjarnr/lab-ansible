[defaults]
inventory = ./inventory
remote_user = ec2_user
# ask_pass = false

# [privilage_escalation]
# become = true
# become_method = ansible.builtin.sudo
# become_user = root
# become_ask_pass = false
